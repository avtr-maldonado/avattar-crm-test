// Este archivo se regenera automáticamente:
//   npm run supabase:types   (con Supabase local corriendo)
// No editar a mano — los cambios se pierden en la siguiente generación.
// Placeholder mientras no se ha corrido el comando por primera vez.

export type Database = {
  public: {
    Tables: Record<string, unknown>;
    Views: Record<string, unknown>;
    Functions: Record<string, unknown>;
  };
};
